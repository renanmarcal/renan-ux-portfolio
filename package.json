{
  "name": "renan-ux-portfolio",
  "private": true,
  "version": "0.0.0",
  "description": "Portfólio estático de Renan Marçal",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "verify": "npm run check && npm run build && node scripts/verify-build.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^7.1.3",
    "typescript": "^6.0.3",
    "wrangler": "^4.112.0"
  }
}
